//! DuckDB DSL expression extensions: operators (and later functions/aggregates)
//! that Diesel does not provide for a third-party backend.

pub mod aggregates;
pub mod functions;
pub mod operators;
